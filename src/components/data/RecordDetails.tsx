import { X } from 'lucide-react';import type { FoodQualityRecord,ParameterKey } from '../../types/quality';import { qualityStandards } from '../../data/qualityStandards';import { parameterStandard } from '../../utils/qualityAssessment';import { formatNumber } from '../../utils/formatting';import { StatusBadge } from '../ui/StatusBadge';
const params:{key:ParameterKey;label:string;unit:string}[]=[{key:'ph',label:'pH',unit:''},{key:'water_activity',label:'Water activity',unit:''},{key:'moisture_percent',label:'Moisture',unit:'%'},{key:'temperature_c',label:'Temperature',unit:'°C'}];
export function RecordDetails({record,onClose}:{record:FoodQualityRecord;onClose:()=>void}){const s=qualityStandards[record.product_name];return <div className="fixed inset-0 z-50 flex justify-end bg-slate-950/30" onClick={onClose}><aside className="h-full w-full max-w-lg overflow-y-auto bg-white p-6 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-navy-600">Sample detail</p><h2 className="mt-1 text-xl font-bold">{record.sample_id}</h2><p className="text-sm text-slate-500">{record.product_name} · {record.batch_number}</p></div><button aria-label="Close" className="rounded-lg p-2 hover:bg-slate-100" onClick={onClose}><X/></button></div><div className="mt-6 grid grid-cols-2 gap-3 rounded-xl bg-slate-50 p-4 text-sm"><span className="text-slate-500">Test date</span><strong className="text-right">{record.test_date}</strong><span className="text-slate-500">Quality score</span><strong className="text-right">{record.quality_score}/100</strong><span className="text-slate-500">Status</span><span className="text-right"><StatusBadge status={record.status!}/></span></div><h3 className="mb-3 mt-7 font-bold">Measured values vs standard</h3>{s?<div className="overflow-hidden rounded-xl border border-slate-200">{params.map(p=>{const value=record[p.key];const issue=record.failed_parameters?.includes(p.label)?'FAIL':record.warning_parameters?.includes(p.label)?'WARNING':value==null?'MISSING':'PASS';return <div key={p.key} className="grid grid-cols-[1fr_auto_auto] items-center gap-3 border-b border-slate-100 p-3 last:border-0"><div><p className="text-sm font-semibold">{p.label}</p><p className="text-xs text-slate-500">Standard {parameterStandard(p.key,s)}</p></div><strong className="text-sm">{formatNumber(value)}{value!=null&&p.unit}</strong><span className={`text-xs font-bold ${issue==='FAIL'?'text-rose-600':issue==='WARNING'?'text-amber-600':issue==='MISSING'?'text-slate-400':'text-emerald-600'}`}>{issue}</span></div>})}</div>:<p className="rounded-xl bg-slate-100 p-4 text-sm text-slate-600">No demonstration standard exists for this product.</p>}<h3 className="mb-2 mt-7 font-bold">Assessment issues</h3><ul className="space-y-2 text-sm">{record.failed_parameters?.map(x=><li key={x} className="rounded-lg bg-rose-50 p-3 text-rose-700">{x} is outside the specification.</li>)}{record.warning_parameters?.map(x=><li key={x} className="rounded-lg bg-amber-50 p-3 text-amber-700">{x} is within 10% of a specification boundary.</li>)}{record.missing_parameters?.map(x=><li key={x} className="rounded-lg bg-slate-100 p-3 text-slate-600">{x} is missing or unsupported.</li>)}{!record.failed_parameters?.length&&!record.warning_parameters?.length&&!record.missing_parameters?.length&&<li className="rounded-lg bg-emerald-50 p-3 text-emerald-700">All required parameters are comfortably within range.</li>}</ul></aside></div>}
