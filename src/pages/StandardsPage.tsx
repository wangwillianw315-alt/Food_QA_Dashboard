import { ShieldCheck } from 'lucide-react';import { qualityStandards,standardsDisclaimer } from '../data/qualityStandards';
export function StandardsPage(){return <div className="space-y-6"><div><p className="eyebrow">Assessment reference</p><h2 className="page-title">Product Quality Standards</h2><p className="page-subtitle">Illustrative parameter limits used by the dashboard assessment engine.</p></div><div className="grid gap-5 md:grid-cols-2">{Object.values(qualityStandards).map(s=><article className="card overflow-hidden" key={s.productName}><div className="flex items-center gap-3 border-b border-slate-200 bg-slate-50 p-5"><span className="rounded-lg bg-navy-100 p-2 text-navy-700"><ShieldCheck size={20}/></span><h3 className="font-bold">{s.productName}</h3></div><dl className="grid grid-cols-2 gap-y-4 p-5 text-sm"><dt className="text-slate-500">pH range</dt><dd className="text-right font-semibold">{s.ph.min}–{s.ph.max}</dd><dt className="text-slate-500">Water activity maximum</dt><dd className="text-right font-semibold">≤ {s.waterActivity.max}</dd><dt className="text-slate-500">Moisture range</dt><dd className="text-right font-semibold">{s.moisture.min}–{s.moisture.max}%</dd><dt className="text-slate-500">Temperature maximum</dt><dd className="text-right font-semibold">≤ {s.temperature.max}°C</dd></dl></article>)}</div><div className="rounded-xl border border-amber-200 bg-amber-50 p-5 text-sm leading-6 text-amber-900"><strong>Important:</strong> {standardsDisclaimer}<br/>The standards in this demonstration dashboard are illustrative only. Real specifications must be defined by the manufacturer, applicable food legislation, validated processes and approved quality documentation.</div></div>}
